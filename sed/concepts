# Cycle
1. Read a line into the pattern space.
2. Process the line as described in the commands.
3. Send output stream the processed line if -n option is specified
4. Flush the pattern space.
-- Repeat the cycle untile EOF.

# Address
Commands가 적용될 범위를 input stream 내에서 지정. 
default는 full input stream.
